import { Routes } from '@angular/router';

// This is a single-page application with all sections rendered on one page
// Navigation is handled via smooth scrolling to section IDs (home, info, insegnanti, progetti, contatto)
// No separate routes are needed - AppComponent renders all sections directly
export const routes: Routes = [];
